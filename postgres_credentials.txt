PostgreSQL Database Credentials
================================
Created: 2025-10-24
Last Updated: 2025-10-24 10:23

Database Information:
---------------------
Database Name: swath_movers
Host: localhost
Port: 5432

User Accounts:
--------------

1. User: aerys
   Password: [The password you entered during migration - check your notes]
   Role: Full privileges on swath_movers database, database owner
   Usage: Development and administration

   Connection command (update with your actual password):
   PGPASSWORD='your_password_here' psql -h localhost -U aerys -d swath_movers

   OR use sudo (no password needed):
   sudo -u postgres psql -d swath_movers

2. User: postgres (superuser)
   Password: N/A (uses peer authentication via sudo)
   Role: PostgreSQL superuser
   Usage: Administrative tasks only

   Connection command:
   sudo -u postgres psql -d swath_movers

Quick Connection Commands:
--------------------------
# Connect as postgres superuser (recommended - no password needed):
sudo -u postgres psql -d swath_movers

# Connect as aerys user (requires password):
PGPASSWORD='your_password' psql -h localhost -U aerys -d swath_movers

# List all databases:
sudo -u postgres psql -l

# List all tables in swath_movers:
sudo -u postgres psql -d swath_movers -c "\dt"

Migration Status:
-----------------
✓ Migration completed successfully on 2025-10-24 10:19
✓ All data migrated from SQLite to PostgreSQL

Database Schema:
----------------
Tables created:
- coordinates (shotpoint data) - 58,641 records
- users (user accounts) - 3 records
- global_deployments - 22,818 records
- swath_1, swath_2, swath_3, swath_4, swath_5, swath_6, swath_7, swath_8 (deployment tables)
- swath_lines (cache table) - 138 records
- swath_boxes (cache table)
- swath_edges (cache table)

Important Notes:
----------------
- Keep this file secure and do not commit it to version control
- The sudoers file for passwordless postgres access is at: /etc/sudoers.d/postgres-nopasswd
- SQLite backups are in this directory with pattern: swath_movers_backup_*.db
- Migration log: migration.log

SECURITY WARNING:
-----------------
This file contains sensitive credentials. Recommended actions:
1. Change passwords to something more secure
2. Add this file to .gitignore
3. Set restrictive file permissions: chmod 600 postgres_credentials.txt
